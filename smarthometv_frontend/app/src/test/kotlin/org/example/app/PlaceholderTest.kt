package org.example.app

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * Placeholder test to ensure the Gradle test task discovers at least one test.
 * This avoids build failure when no other unit tests are present in the app module.
 */
class PlaceholderTest {

    @Test
    fun placeholder() {
        assertTrue(true)
    }
}
